<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
     http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
     http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName" value="${jdbc_driverClassName}" />

		<property name="url" value="jdbc:mysql://${jdbc_host}:${jdbc_port}/${jdbc_db_name}?useUnicode=true&amp;characterEncoding=UTF-8&amp;zeroDateTimeBehavior=convertToNull" />

		<property name="username" value="${jdbc_db_user}" />

		<property name="password" value="${jdbc_db_pwd}" />

		<!-- 指定连接数据库连接池的最大连接数 -->

		<!-- <property name="maxPoolSize" value="20"/> -->

		<!-- 指定连接数据库连接池的最小连接数 -->

		<!-- <property name="minPoolSize" value="1"/> -->

		<!-- 指定连接数据库连接池的初始化连接数 -->

		<!-- <property name="initialPoolSize" value="1"/> -->

		<!-- 指定连接数据库连接池的连接的最大空闲时间 -->

		<!-- <property name="maxIdleTime" value="20"/> -->
	</bean>

	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="typeAliasesPackage" value="com.lezo.iscript.yeam.tasker.dto" />
		<property name="configLocation" value="classpath:mybatis-config-tasker.xml" />
	</bean>

	<bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg index="0" ref="sqlSessionFactory" />
	</bean>

	<bean id="txManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<!-- Mapper接口所在包名，Spring会自动查找其下的Mapper -->
	<!-- <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage" value="com.lezo.iscript.yeam.tasker.dao" />
	</bean> -->
</beans>  